language: cpp

dist: xenial

services:
  - docker

before_install:
  - docker build -t hevx.el7 -f test/Dockerfile.el7 .

script:
  - docker run hevx.el7 scl enable devtoolset-7 rh-python36 -- ctest3 -VV -S CTestScript.cmake
