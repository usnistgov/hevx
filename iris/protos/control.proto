syntax = "proto3";
package iris.Control;

import "displays.proto";
import "window.proto";
import "shadertoy.proto";

message Control {
  oneof type {
    Displays displays = 2;
    Window window = 3;
	  ShaderToy shaderToy = 20;
  }
}
